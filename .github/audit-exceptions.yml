version: 1
exceptions:
  - package: xlsx
    advisory: "GHSA-4r6h-8v6p-xvw6"
    severity: high
    reason: "Admin export only; no fix available on the npm registry (npm stops at 0.18.5, SheetJS ships fixes via cdn.sheetjs.com only) (CVE-2023-30533)"
    mitigation: "Load only on export via dynamic import; restrict export permissions and data scope"
    expires_on: "2026-11-30"
    owner: "security@your-domain"
  - package: xlsx
    advisory: "GHSA-5pgg-2g8v-p4x9"
    severity: high
    reason: "Admin export only; no fix available on the npm registry (npm stops at 0.18.5, SheetJS ships fixes via cdn.sheetjs.com only) (CVE-2024-22363)"
    mitigation: "Load only on export via dynamic import; restrict export permissions and data scope"
    expires_on: "2026-11-30"
    owner: "security@your-domain"
