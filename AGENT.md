# AGENT.md

本仓库的协作约定。供自动化编码 agent 及贡献者遵循。

每条只留**一句话钩子**，细节进 `docs/notes/` 的独立文件，用超链接点进去看。

## 开发流程（spec 驱动）

feature 级改动默认按五步推进，不必逐步征询：**先写 spec → 对着 spec 实现 → 写常规测试 → 真实容器环境 + 生产形态数据的边界/并发/幂等测试 → 回头逐条核对 spec 是否一一对上**。每阶段以「编译 + 静态检查全绿」为门槛；破坏性 / 改钱的迁移延后到上线前（副本演练 + 对账）；用户可见页面不暴露内部机制。

→ 完整五步、贯穿原则与「为什么」：[docs/notes/dev-workflow.md](docs/notes/dev-workflow.md)

## 工程笔记保存流程

遇到「踩过的坑 / 非显而易见的约定 / 排查结论」，按下面分流保存，别让它只活在一次对话里：

1. **判断密级。** 通用工程类（构建、迁移机制、ORM/SQL 陷阱、前端样式坑、测试环境搭建等）→ 进本仓 `docs/notes/`。**含密钥 / 服务器部署细节 / 业务内部机制（账号池、订阅转 API 等）→ 绝不进本公开仓**，留在本地私有记忆里。
2. **写细节文件。** 在 `docs/notes/` 下新建一个 kebab-case 的 `.md`，一文件一主题。正文讲清**现象 + 根因 + 怎么修 / 怎么用 + 为什么**，给出相关文件路径、commit 等可追溯锚点。
3. **在本文件登记钩子。** 回到下面「工程笔记索引」，加一行：很短的一句话描述 + 指向该细节文件的超链接。`AGENT.md` 只放钩子，不放正文。
4. **先查重再写。** 已有覆盖同一主题的文件就更新它，别新建重复条目；发现某条已过时（文件 / 函数 / 标志已不存在）就修正或删除。

## 工程笔记索引

- [DB 迁移重号约定](docs/notes/db-migration-duplicate-numbers.md) —— 迁移数字前缀重号是有意且安全的，按完整文件名跟踪/排序，**切勿为连号而重命名已有迁移**。
- [可空列原始 SQL 扫描陷阱](docs/notes/nullable-column-rawsql-scan.md) —— 列改可空后，手写原始 SQL 扫到 `int64`/`string` 会在 NULL 行崩（P0 资损）；改用 `sql.NullXxx`，并补集成测试覆盖 NULL 行。
- [Tailwind 透明度 vs scoped 自定义色](docs/notes/tailwind-scoped-color-opacity.md) —— `<style scoped>` 里定义的 CSS 变量色不吃 `/NN` 透明度修饰符，会**静默失效**；要弱化得显式补 `color-mix` 转义类。
